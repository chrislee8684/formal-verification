//////////////////////////////////////////////////////////////////////
// SAFETY PROPERTIES (PCTL)
//////////////////////////////////////////////////////////////////////

// Bid is always strictly less than ask
P>=1 [ G (bid < ask) ]

// Inventory (net position) always within risk limit
P>=1 [ G (inv <= MAX_LIMIT & inv >= -MAX_LIMIT) ]
