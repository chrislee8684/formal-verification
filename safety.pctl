//////////////////////////////////////////////////////////////////////
// SAFETY PROPERTIES
//////////////////////////////////////////////////////////////////////

// Bid is always strictly less than ask (no arbitrage loss)
P>=1 [ G (bid < ask) ]

// Inventory always within risk limits
P>=1 [ G (inv <= MAX_LIMIT & inv >= -MAX_LIMIT) ]
